import type { Database } from './database.types'

// For now, just use Database directly
// In the future, we can extend specific function return types if needed
export type CustomDatabase = Database
